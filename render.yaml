databases:
  - name: postgres
    ipAllowList: []
    databaseName: messageMe_render
    user: messageMe_render

services:
  - type: web
    name: messageMe
    env: ruby
    buildCommand: './render-build.sh'
    startCommand: 'bundle exec rails s'
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgres
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
